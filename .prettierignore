dist
node_modules
.astro
.wrangler
.lighthouseci
package-lock.json
public
playwright-report
test-results
tests/**/__screenshots__

# Generated at build time by scripts/generate-placeholders.mjs. Machine-written
# JSON, one long line of base64 blurs; prettier must never rewrite it.
src/lib/coverPlaceholders.json

# Rendered-HTML parity baselines (scripts/page-parity.mjs). They are byte
# snapshots of built pages; prettier must never rewrite them.
scripts/.parity

# NOTE (2026-09-06): scripts/sync-check.mjs used to be listed here, because
# prettier would rewrite its quoting and `npm run sync-check` would then report
# DRIFT. It was formatted in the starter instead, in one family-wide pass, and
# this repo took the formatted bytes (PORTS.md card 36). Prettier is now a fixed
# point on it, so format:check is a second guard on the canonical text rather
# than a threat to it. Do not re-add it.

# Wrangler's generated runtime types.
worker-configuration.d.ts
