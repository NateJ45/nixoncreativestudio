{
  "//": "Lighthouse CI config. Runs against the built static output so the dev-server SSR-dedup quirks never affect scores. Accessibility is a hard gate (the site holds 100 and that bar must not regress); performance / best-practices / SEO are warnings so normal CI variance doesn't block a PR while still surfacing drops. Run locally with: npm run build && npx lhci autorun",
  "ci": {
    "collect": {
      "staticDistDir": "./dist/client",
      "numberOfRuns": 1,
      "settings": {
        "skipAudits": ["uses-http2", "canonical"]
      }
    },
    "assert": {
      "assertions": {
        "categories:accessibility": ["error", { "minScore": 1 }],
        "categories:best-practices": ["warn", { "minScore": 0.95 }],
        "categories:seo": ["warn", { "minScore": 0.95 }],
        "categories:performance": ["warn", { "minScore": 0.85 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
